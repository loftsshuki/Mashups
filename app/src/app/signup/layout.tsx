import type { Metadata } from "next"

export const metadata: Metadata = { title: "Start free", description: "Create a Mashups campaign workspace.", alternates: { canonical: "/signup" } }
export default function SignupLayout({ children }: { children: React.ReactNode }) { return children }
