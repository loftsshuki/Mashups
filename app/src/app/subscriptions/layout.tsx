import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Subscriptions",
  description: "Manage your creator subscriptions on Mashups.",
}

export default function SubscriptionsLayout({ children }: { children: React.ReactNode }) {
  return children
}
